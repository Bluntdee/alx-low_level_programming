#include <main.h>
/**
 * _putchar - writes the character to stdout
 * @c:The character to print
 * Return: On a success 1
 * On error, -1 is returned, aand errno is set appropriately
 */

int _putchar(char c)
{

	return (write(1, &C, 1));
}


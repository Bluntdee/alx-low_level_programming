#include "main.h"
/**
 * _abs - print the absolute value of number
 * @n: number to be printed
 * Return: value of n
 */
int _abs(int n)
{
	if (n > 0)
	{
	return (n);
	}
	else if (n < 0)
	{
	n = (n * -1);
	return (n);
	}
	else
	{
	return (n);
	}
	return (n);
}
